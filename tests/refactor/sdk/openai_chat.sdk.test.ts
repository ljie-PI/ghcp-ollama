import { describe, it } from "vitest";

describe("RM-09 official OpenAI Chat SDK", () => {
  it.skip("manual-only: Chat Completions non-stream, stream, error class/request ID, and cancellation", () => {
    // GHC_GATEWAY_SDK_TESTS=1 npm run test:sdk:refactor -- tests/refactor/sdk/openai_chat.sdk.test.ts
  });
});
